eval $("ssh-agent")

ssh-add ~/.ssh/school

ssh ubuntu@54.173.114.195#!/usr/bin/env bash
# Server-01 login commands

# Setting up the ssh-agent
eval $("ssh-agent")

# Path to IdentityFile
ssh-add ~/.ssh/school

# Username and Ip Adress
ssh ubuntu@34.227.89.226
